version: '3'
services:
  web:
    image: amazon/amazon-ecs-sample
    ports:
      - "80:80"
    logging:
      driver: awslogs
      options: 
        awslogs-group: vonder
        awslogs-region: $AWS_DEFAULT_REGION
        awslogs-stream-prefix: web